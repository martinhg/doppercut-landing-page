---
// src/pages/index.astro
import { defaultCountry } from '../i18n/config';

// Esta es una redirección del lado del servidor.
// El usuario nunca verá una página en blanco, será enviado instantáneamente a /ar (o lo que definas como default)
return Astro.redirect(`/${defaultCountry}`);
---
